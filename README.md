### errors / warnings
Index p_gutter = -1 is out of bounds (gutters.size() = 4)
```
# engine bug, sometimes happens when switching between multiple IDEs i.e. vscode and godot's built-in
Godot Editor top menu:
- Editor > Editor Settings...In the left-hand menu sidebar, scroll down to Text Editor and select Appearance.Scroll through the right-side properties until you find Highlight Type Safe Lines.Uncheck / Turn off this setting.
```